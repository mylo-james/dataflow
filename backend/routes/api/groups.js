const router = require('express').Router();

/* Write an  get route to '/:id(\\d+)' that...
    1. Uses the asyncHandler middleware from 'express-async-handler'
    2. Parses the id from the req.params
    3. Finds all the users where instructorId is equal to the id
    4. Send a json response of all the users you found                   TODO */

module.exports = router;
