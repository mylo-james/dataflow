// Action Type Definitions

//Action Creators

//Thunks

//Reducer
const userReducer = (state = [], action) => {
    switch (action.type) {
        default:
            return state;
    }
};

export default userReducer;
